services:
  web:
    image: nginx:1.27-alpine
    ports:
      - "8080:80"
    volumes:
      - ./lab/nginx-default.conf:/etc/nginx/conf.d/default.conf:ro

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--protected-mode", "no"]
    ports:
      - "6379:6379"
